naftiko: 1.0.0-alpha2
info:
  label: Azure Virtual Network REST API — Operations
  description: 'Azure Virtual Network REST API — Operations. 1 operations. Lead operation: Azure Virtual Network REST API
    List operations. Self-contained Naftiko capability covering one Microsoft Azure Networking business surface.'
  tags:
  - Microsoft Azure Networking
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_NETWORKING_API_KEY: MICROSOFT_AZURE_NETWORKING_API_KEY
capability:
  consumes:
  - type: http
    namespace: microsoft-azure-networking-operations
    baseUri: https://management.azure.com
    description: Azure Virtual Network REST API — Operations business capability. Self-contained, no shared references.
    resources:
    - name: providers-Microsoft.Network-operations
      path: /providers/Microsoft.Network/operations
      operations:
      - name: listoperations
        method: GET
        description: Azure Virtual Network REST API List operations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_AZURE_NETWORKING_API_KEY}}'
  exposes:
  - type: rest
    namespace: microsoft-azure-networking-operations-rest
    port: 8080
    description: REST adapter for Azure Virtual Network REST API — Operations. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/providers/microsoft-network/operations
      name: providers-microsoft-network-operations
      description: REST surface for providers-Microsoft.Network-operations.
      operations:
      - method: GET
        name: listoperations
        description: Azure Virtual Network REST API List operations
        call: microsoft-azure-networking-operations.listoperations
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: microsoft-azure-networking-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Azure Virtual Network REST API — Operations. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: azure-virtual-network-rest-api
      description: Azure Virtual Network REST API List operations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: microsoft-azure-networking-operations.listoperations
      outputParameters:
      - type: object
        mapping: $.
